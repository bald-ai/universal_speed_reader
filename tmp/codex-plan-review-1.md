VERDICT: CHANGES_NEEDED
CRITIQUE:
- The proposed import-service gate is not “immediately after parse” in current code: source persistence and possible metadata mutation occur before the existing paragraph checks. The plan must name the exact insertion point directly after `withTimeout(...)`.
- The parser gate is the only gate that prevents the `Math.max(...paragraphWordCounts)` crash. An import-only check cannot help because `parseBookBytes` validates before returning.
- Constant ownership is unresolved. `bookImportService.ts` is the wrong owner because parser validation must enforce the limit independently. The parser should own/export the constant; import may consume it.
- `importDiagnostics.ts` does not map diagnostics to `ImportFailure` categories. It only selects `hardFailure`; current `bookImportService.ts` always converts that to `"Book content not reliable"`. The plan’s proposed `"Book too large"` category requires an explicit import-service branch or mapper.
- Adding the diagnostic outside `SOFT_DIAGNOSTIC_CODES` correctly makes it hard, but the plan should explicitly require `severity: "failure"`; code omission alone is insufficient.
- A naïve early return from `validateParserOutput` could discard or mishandle pre-existing `book.diagnostics`. The plan must preserve and deduplicate them while adding the limit diagnostic.
- The restore claim is unsupported and likely wrong as written. Current code can call `clearBookContent` before parsing, so `purgeOnHardFailure: false` does not inherently mean prior content survives. The plan must verify and test actual restore behavior rather than assert “keep-prior-content.”
- “20k without stack overflow” is not a realistic regression test for the spread crash and may pass with the old implementation. It also does not exercise the safe-max code if oversized inputs short-circuit.
- The import test must exercise the real placement of the gate and assert that source persistence, metadata patching, diagnostics classification, and content replacement do not occur after an oversized parse—not merely that chunk persistence is absent.
- “Same pattern wherever similar spreads exist ... if touched” is vague scope creep and contradicts the restricted, concrete change.
- The plan incorrectly leaves core behavior as reviewer open questions: duplicate enforcement, failure category, and constant sharing must be resolved before implementation.
REQUIRED_PLAN_EDITS:
- Replace the constant section with: “Define and export `MAX_BOOK_PARAGRAPHS = 50_000` in the parser layer, alongside parser resource-limit validation. `bookImportService.ts` imports this constant for its defensive product gate. Do not define the canonical constant in the import service.”
- Replace the parser-gate text with: “At the start of `validateParserOutput`, before `validateText`, detect `paragraphs.length > MAX_BOOK_PARAGRAPHS`, add a `severity: "failure"` `too_many_paragraphs` diagnostic, merge/deduplicate it with existing `book.diagnostics`, and return without running remaining validators.”
- Replace the import ordering with: “Insert the defensive count check immediately after `await withTimeout(...)` returns and before `ensureTaskSourcePersisted`, metadata patching, empty/ID checks, diagnostic classification, or persistence.”
- Specify: “The defensive import check throws `new ImportFailure("Book too large", "Book too large: this book has X paragraphs; maximum supported is 50,000")`. The parser diagnostic uses the same factual count and limit. Do not claim `importDiagnostics.ts` creates this category.”
- Replace the restore/purge assertion with: “Add tests for new-import and Restore-to-original paths. Establish whether content is cleared before parsing in each path. New-import oversize failure must purge its provisional row. Restore failure must preserve the previously completed content; if current pre-parse clearing prevents that, the implementation must change the clearing/rollback ordering rather than relying on `purgeOnHardFailure: false`.”
- Replace the tests with: “Test `validateParserOutput` at 50,000 and 50,001 paragraphs; at 50,001 assert one limit failure, preservation of pre-existing diagnostics, and absence of diagnostics that only later validators could produce. Test the safe maximum calculation directly or with an in-limit fixture that reaches it; do not use 20k ‘no throw’ as proof of the former spread crash. Add an import-service test asserting the exact `ImportFailure` category/message and zero post-parse source/content/metadata writes.”
- Delete the vague `pdf-content if touched` instruction.
- Remove all three open questions by adopting the decisions above.